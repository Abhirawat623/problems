console.log("hello");

setTimeout(function(){
   console.log("wait here");},4000);

console.log("be be be bee");
//async function gives return (promise) while await waits for the promise to be settled

            //we can use await for const and function too but async used before function instead of res=> res("resolved");
