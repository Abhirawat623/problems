const name="prakash";
const arrayofChar =name.split("");// break the string into the objects of any arraw{ always do "" to break into letters}
arrayofChar.reverse(); //reverse the orders
let reversedWord=arrayofChar.join("");// same here to this to add the letters of the world

console.log(reversedWord);